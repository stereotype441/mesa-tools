#!/bin/bash
set -e
platform checkactive
cd ~/dri2proto
git tag -d install || true
git tag install `git-commit-working-tree`
make install
cd ~/glproto
git tag -d install || true
git tag install `git-commit-working-tree`
make install
