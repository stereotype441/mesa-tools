#!/bin/bash

# Temporarily disabled because "make check" is currently broken in Mesa
exit 0

set -e
platform checkactive
cd ~/mesa
git tag -d check || true
git tag check `git-commit-working-tree`
make check
