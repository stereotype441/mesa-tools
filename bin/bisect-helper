#!/bin/bash
PIGLIT_TEST="/home/pberry/.platform/piglit-mesa/piglit/build/bin/shader_runner tests/spec/glsl-1.20/execution/clipping/fixed-clip-enables.shader_test -auto"
platform binst mesa || platform cbinst mesa || exit 125
cd ~/piglit; bash -c "platform using piglit-mesa $PIGLIT_TEST" || exit 1
exit 0
